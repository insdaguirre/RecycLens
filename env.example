# OpenAI API Key (required)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Mapbox Access Token (required for map functionality)
# Get your access token from https://account.mapbox.com/access-tokens/
# Note: Vite requires VITE_ prefix for frontend environment variables
VITE_MAPBOX_ACCESS_TOKEN=pk.ey...

# RAG microservice (optional but recommended for local-regulation sources)
# Run the Python service in ./rag_service on port 8001
RAG_SERVICE_URL=http://localhost:8001

# Server Port (optional, defaults to 3001)
PORT=3001

# Environment (optional, defaults to development)
NODE_ENV=development
